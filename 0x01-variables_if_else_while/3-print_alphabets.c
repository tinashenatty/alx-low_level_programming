#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry print alphabet
 *
 * Return: Always 0
 */


int main(void)/* your code goes there */
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	for (ch = 'A'; ch <= 'Z'; ch++)
	putchar('\n');


	return (0);
}
