#include <stdlib.h>
#include <stdio.h>
/**
 * main - Prints the alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char t;

	t = 'a';
	while
		(t <= 'z')
		{
			putchar(t);
			t++;
		}
	putchar("\n");
	return (0);
}
